# 🚀 Сервер для общего рейтинга IQ теста

Этот сервер позволяет всем пользователям видеть общий рейтинг результатов теста IQ.

## 📋 Требования

- Node.js 14.0.0 или выше
- npm или yarn

## 🔧 Установка

1. Перейдите в папку сервера:
```bash
cd server
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите сервер:
```bash
npm start
```

Для разработки с автоперезагрузкой:
```bash
npm run dev
```

Сервер будет доступен по адресу: `http://localhost:3000`

## 🌐 Развертывание на Render.com (рекомендуется)

1. Зарегистрируйтесь на [Render.com](https://render.com)

2. Создайте новый **Web Service**

3. Подключите ваш GitHub репозиторий

4. Настройки:
   - **Name**: `iq-test-server` (или любое другое имя)
   - **Environment**: `Node`
   - **Build Command**: `cd server && npm install`
   - **Start Command**: `cd server && npm start`
   - **Root Directory**: оставьте пустым или укажите корень репозитория

5. Нажмите **Create Web Service**

6. После развертывания скопируйте URL (например: `https://iq-test-server.onrender.com`)

7. Обновите URL сервера в `js/server-api.js`:
```javascript
serverUrl: 'https://your-server-url.onrender.com'
```

## 🌐 Развертывание на Railway

1. Зарегистрируйтесь на [Railway.app](https://railway.app)

2. Создайте новый проект и подключите GitHub репозиторий

3. Добавьте новый сервис и выберите папку `server`

4. Railway автоматически определит Node.js проект

5. После развертывания скопируйте URL и обновите в `js/server-api.js`

## 🌐 Развертывание на Heroku

1. Установите [Heroku CLI](https://devcenter.heroku.com/articles/heroku-cli)

2. Войдите в Heroku:
```bash
heroku login
```

3. Создайте приложение:
```bash
cd server
heroku create your-app-name
```

4. Разверните:
```bash
git push heroku main
```

5. Откройте приложение:
```bash
heroku open
```

## 📡 API Endpoints

### POST `/api/results`
Сохраняет результат теста на сервере.

**Тело запроса:**
```json
{
  "iq": 120,
  "userName": "Иван",
  "correctAnswers": 25,
  "totalQuestions": 35,
  "difficulty": "medium",
  "testMode": "standard"
}
```

**Ответ:**
```json
{
  "success": true,
  "id": "1234567890_abc123"
}
```

### GET `/api/leaderboard?filter=all`
Получает рейтинг пользователей.

**Параметры:**
- `filter`: `all`, `month`, `week`

**Ответ:**
```json
[
  {
    "rank": 1,
    "userName": "Иван",
    "iq": 150,
    "testCount": 5,
    "timestamp": "2026-02-11T12:00:00.000Z"
  }
]
```

### GET `/api/stats`
Получает общую статистику.

**Ответ:**
```json
{
  "totalTests": 100,
  "averageIQ": 110,
  "maxIQ": 150,
  "minIQ": 80,
  "totalUsers": 50
}
```

### GET `/api/health`
Проверка работоспособности сервера.

**Ответ:**
```json
{
  "status": "ok",
  "timestamp": "2026-02-11T12:00:00.000Z"
}
```

## ⚙️ Настройка клиента

После развертывания сервера обновите URL в `js/server-api.js`:

```javascript
serverUrl: 'https://your-server-url.onrender.com'
```

Или установите переменную окружения перед загрузкой скрипта:

```html
<script>
    window.SERVER_URL = 'https://your-server-url.onrender.com';
</script>
<script src="js/server-api.js"></script>
```

## 🔒 Безопасность

Для продакшена рекомендуется:

1. Добавить rate limiting (ограничение запросов)
2. Добавить валидацию данных
3. Использовать базу данных вместо JSON файла
4. Добавить аутентификацию для административных операций

## 📝 Структура данных

Результаты хранятся в `data/results.json`:

```json
[
  {
    "id": "1234567890_abc123",
    "iq": 120,
    "userName": "Иван",
    "timestamp": "2026-02-11T12:00:00.000Z",
    "correctAnswers": 25,
    "totalQuestions": 35,
    "difficulty": "medium",
    "testMode": "standard"
  }
]
```

## 🐛 Решение проблем

### Сервер не запускается
- Проверьте версию Node.js: `node --version` (должна быть >= 14.0.0)
- Удалите `node_modules` и установите заново: `rm -rf node_modules && npm install`

### Ошибки CORS
- Убедитесь, что `cors` установлен: `npm install cors`
- Проверьте, что сервер возвращает правильные заголовки CORS

### Данные не сохраняются
- Проверьте права на запись в папку `data/`
- Проверьте логи сервера на наличие ошибок

## 📞 Поддержка

При возникновении проблем проверьте логи сервера и убедитесь, что все зависимости установлены.
