# ⚙️ Настройки Render.com для сервера

## 📋 Точные настройки для развертывания

При создании **Web Service** в Render.com используйте следующие настройки:

### Основные настройки

| Поле | Значение | Примечание |
|------|----------|------------|
| **Name** | `iq-test-server` | Или любое другое имя |
| **Region** | Выберите ближайший | Например: Frankfurt, Oregon |
| **Branch** | `main` | Или `master` в зависимости от вашей ветки |
| **Root Directory** | **ПУСТО** или `.` | Оставьте пустым |
| **Base Directory** | **ПУСТО** или `.` | Оставьте пустым |
| **Environment** | `Node` | Обязательно выберите Node |
| **Build Command** | `cd server && npm install` | Установка зависимостей |
| **Start Command** | *(не видно в интерфейсе)* | Render автоматически использует `npm start` из package.json |
| **Publish Directory** | **ПУСТО** | Не используется для Node.js |
| **Functions Directory** | **ПУСТО** | Не используется |

### ✅ Если нет поля "Start Command"

**Вариант 1 (рекомендуется):** Укажите только установку зависимостей:
```
Build Command: cd server && npm install
```
Render автоматически найдет и запустит `npm start` из `package.json` (там есть скрипт "start": "node server.js")

**Вариант 2:** Если нужно указать все в Build Command:
```
Build Command: cd server && npm install && npm start
```

**Важно:** В `package.json` уже есть скрипт `"start": "node server.js"`, поэтому Render автоматически его найдет и запустит после установки зависимостей.

### ⚠️ Важно

- **Publish Directory** и **Functions Directory** используются только для статических сайтов и serverless функций
- Для Node.js сервера эти поля должны быть **ПУСТЫМИ**
- **Root Directory** и **Base Directory** можно оставить пустыми или указать `.` (точка)

### 📸 Скриншот настроек

```
┌─────────────────────────────────────┐
│ Name: iq-test-server               │
│ Region: Frankfurt (EU)             │
│ Branch: main                       │
│ Root Directory: [пусто]            │
│ Base Directory: [пусто]            │
│                                    │
│ Environment: Node                 │
│                                    │
│ Build Command:                     │
│ cd server && npm install           │
│                                    │
│ Start Command:                     │
│ (автоматически: npm start)         │
│ Render найдет из package.json      │
│                                    │
│ Publish Directory: [пусто]         │
│ Functions Directory: [пусто]       │
└─────────────────────────────────────┘
```

### ✅ После развертывания

1. Дождитесь завершения развертывания (2-3 минуты)
2. Скопируйте URL сервера (например: `https://iq-test-server.onrender.com`)
3. Обновите URL в `index.html` или `js/server-api.js`

### 🔍 Проверка работы

Откройте в браузере:
```
https://your-server-url.onrender.com/api/health
```

Должен вернуться JSON:
```json
{
  "status": "ok",
  "timestamp": "2026-02-11T..."
}
```
