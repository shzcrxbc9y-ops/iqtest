# 🔒 Решение проблемы с предупреждением безопасности

## Проблема
Браузер показывает предупреждение: "Этот сайт может попытаться обманным способом заставить Вас выполнить небезопасные действия..."

## Причины

### 1. Локальное открытие файла (file:// протокол)
Если вы открываете `index.html` напрямую через файловый менеджер, браузер блокирует его как потенциально опасный.

**Решение:**
- ✅ Используйте локальный веб-сервер (см. ниже)
- ✅ Или опубликуйте на GitHub Pages (автоматически HTTPS)

### 2. Отсутствие HTTPS на GitHub Pages
GitHub Pages автоматически предоставляет HTTPS, но иногда нужно подождать несколько минут после первой публикации.

## ✅ Решения

### Вариант 1: Локальный веб-сервер (для разработки)

#### Python (если установлен):
```bash
cd C:\Users\User\Desktop\iq
python -m http.server 8000
```
Затем откройте: `http://localhost:8000`

#### Node.js (если установлен):
```bash
cd C:\Users\User\Desktop\iq
npx http-server -p 8000
```

#### VS Code:
Установите расширение "Live Server" и запустите через правую кнопку мыши на `index.html` → "Open with Live Server"

### Вариант 2: GitHub Pages (рекомендуется)

1. Убедитесь, что сайт опубликован на GitHub
2. Включите GitHub Pages:
   - Settings → Pages
   - Source: Branch `main`, Folder `/ (root)`
   - Сохраните
3. Подождите 1-2 минуты
4. Откройте: `https://shzcrxbc9y-ops.github.io/iqtest/`

GitHub Pages автоматически предоставляет HTTPS сертификат.

### Вариант 3: Обход предупреждения (не рекомендуется)

⚠️ **Только для локального тестирования!**

В Chrome:
1. Нажмите на предупреждение
2. Выберите "Дополнительно"
3. Нажмите "Перейти на сайт (небезопасно)"

**Не используйте это для публичного сайта!**

## 🔐 Что было добавлено для безопасности

1. ✅ Мета-теги безопасности в `index.html`
2. ✅ Content Security Policy (CSP)
3. ✅ Файл `robots.txt` для поисковых систем
4. ✅ Файл `.well-known/security.txt` для верификации

## 📝 Проверка

После публикации на GitHub Pages проверьте:
- ✅ URL начинается с `https://`
- ✅ В адресной строке есть значок замка 🔒
- ✅ Нет предупреждений безопасности

## 🆘 Если проблема осталась

1. Очистите кэш браузера (Ctrl+Shift+Delete)
2. Проверьте консоль браузера (F12) на ошибки
3. Убедитесь, что все файлы загружены на GitHub
4. Подождите 5-10 минут после первой публикации (для активации HTTPS)

---

**Важно:** Для публичного сайта всегда используйте HTTPS (GitHub Pages, Netlify, Vercel и т.д.)
