# 🔧 Настройка клиента для работы с сервером

После развертывания сервера на Render.com необходимо обновить URL сервера в клиентском коде.

## 📝 Способ 1: Обновление в `index.html` (рекомендуется)

Откройте файл `index.html` и найдите строку ~745:

```html
<!-- Настройка URL сервера (замените на ваш URL после развертывания) -->
<script>
    // Раскомментируйте и укажите URL вашего сервера:
    // window.SERVER_URL = 'https://your-server-url.onrender.com';
</script>
```

**Замените на:**
```html
<script>
    window.SERVER_URL = 'https://ваш-сервер.onrender.com';
</script>
```

**Пример:**
```html
<script>
    window.SERVER_URL = 'https://iq-test-server.onrender.com';
</script>
```

## 📝 Способ 2: Обновление в `js/config.js`

Альтернативный способ — обновить URL в `js/config.js`:

**Найдите строку:**
```javascript
SERVER_URL: null, // Раскомментируйте и укажите URL: 'https://your-server.onrender.com'
```

**Замените на:**
```javascript
SERVER_URL: 'https://ваш-сервер.onrender.com',
```

## ✅ Проверка работы

После обновления URL:

1. Откройте сайт в браузере
2. Откройте консоль разработчика (F12)
3. Пройдите тест
4. Проверьте консоль — должны быть сообщения о подключении к серверу
5. Проверьте страницу "Рейтинг" — должны отображаться данные с сервера

## 🔍 Как найти URL вашего сервера на Render.com

1. Зайдите на https://dashboard.render.com
2. Выберите ваш сервис (Web Service)
3. Скопируйте URL из раздела "Service Details"
4. URL будет выглядеть как: `https://your-service-name.onrender.com`

## ⚠️ Важно

- Убедитесь, что сервер развернут и работает (проверьте `/api/health`)
- URL должен начинаться с `https://`
- Не добавляйте `/api` в конце URL — это добавляется автоматически
- После обновления URL не забудьте закоммитить и запушить изменения в Git

## 🚀 После настройки

После обновления URL выполните:

```bash
git add index.html
git commit -m "Update server URL"
git push
```

Netlify автоматически обновит сайт через несколько минут.
