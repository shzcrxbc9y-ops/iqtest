# 🌐 Настройка сервера для общего рейтинга

## Быстрый старт

### Вариант 1: Render.com (самый простой)

1. Зайдите на [render.com](https://render.com) и зарегистрируйтесь
2. Нажмите **New +** → **Web Service**
3. Подключите ваш GitHub репозиторий
4. Настройки:
   - **Name**: `iq-test-server` (или любое другое имя)
   - **Region**: выберите ближайший (например, Frankfurt)
   - **Branch**: `main` (или `master`)
   - **Root Directory**: оставьте **ПУСТЫМ** (или укажите `.` - точка)
   - **Environment**: `Node`
   - **Build Command**: `cd server && npm install`
   - **Start Command**: `cd server && npm start`
   - **Publish Directory**: оставьте **ПУСТЫМ** (не используется для Node.js)
   - **Functions Directory**: оставьте **ПУСТЫМ** (не используется)
   - **Base Directory**: оставьте **ПУСТЫМ** (или укажите `.` - точка)
5. Нажмите **Create Web Service**
6. Дождитесь развертывания (2-3 минуты)
7. Скопируйте URL (например: `https://iq-test-server.onrender.com`)

**Важно:** 
- Для Node.js сервера **Publish Directory** и **Functions Directory** должны быть **ПУСТЫМИ**
- **Base Directory** можно оставить пустым или указать `.` (точка)
- **Root Directory** можно оставить пустым или указать `.` (точка)

### Вариант 2: Railway.app

1. Зайдите на [railway.app](https://railway.app)
2. Создайте новый проект
3. Подключите GitHub репозиторий
4. Добавьте сервис и выберите папку `server`
5. Railway автоматически определит Node.js и развернет

### Вариант 3: Heroku

```bash
cd server
heroku create your-app-name
git push heroku main
```

## 🔧 Обновление клиента

После развертывания сервера обновите URL в `js/server-api.js`:

Найдите строку:
```javascript
serverUrl: window.SERVER_URL || 'https://your-server-url.herokuapp.com',
```

Замените на ваш URL:
```javascript
serverUrl: window.SERVER_URL || 'https://your-actual-server-url.onrender.com',
```

Или установите переменную в `index.html` перед загрузкой скриптов:

```html
<script>
    window.SERVER_URL = 'https://your-server-url.onrender.com';
</script>
```

## ✅ Проверка работы

1. Откройте консоль браузера (F12)
2. Перейдите на страницу "Рейтинг"
3. Проверьте, что нет ошибок CORS
4. Пройдите тест и проверьте, что результат появился в общем рейтинге

## 📊 Что изменилось

- ✅ Результаты теперь сохраняются на сервере
- ✅ Все пользователи видят общий рейтинг
- ✅ Рейтинг работает даже если пользователь очистил Local Storage
- ✅ Fallback на локальное хранилище, если сервер недоступен

## 🐛 Решение проблем

**Ошибка CORS:**
- Убедитесь, что сервер запущен и доступен
- Проверьте URL в `server-api.js`

**Результаты не отправляются:**
- Откройте консоль браузера (F12) и проверьте ошибки
- Убедитесь, что URL сервера правильный

**Рейтинг пустой:**
- Проверьте, что сервер работает: откройте `https://your-server-url/api/health`
- Проверьте логи сервера на наличие ошибок
