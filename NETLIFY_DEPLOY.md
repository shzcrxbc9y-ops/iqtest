# 🚀 Обновление сайта в Netlify через GitHub

## ✅ Автоматический деплой (рекомендуется)

Netlify может автоматически обновлять сайт при каждом push в GitHub!

### Шаг 1: Подключите GitHub к Netlify

1. Войдите в [Netlify](https://app.netlify.com/)
2. Нажмите **"Add new site"** → **"Import an existing project"**
3. Выберите **"GitHub"** и авторизуйтесь
4. Найдите репозиторий: `shzcrxbc9y-ops/iqtest`
5. Нажмите **"Connect"**

### Шаг 2: Настройте деплой

**Build settings:**
- **Branch to deploy:** `main`
- **Base directory:** (оставьте пустым или `/`)
- **Build command:** (оставьте пустым - статический сайт)
- **Publish directory:** `/` (корневая папка)
- **Functions directory:** (оставьте пустым - не используется)

**Важно:** 
- ✅ **Publish directory** = `/` (корневая папка проекта)
- ✅ **Base directory** = оставьте пустым
- ✅ **Functions directory** = оставьте пустым (для статического сайта не нужен)

### Шаг 3: Нажмите "Deploy site"

Netlify автоматически:
- ✅ Склонирует ваш репозиторий
- ✅ Опубликует сайт
- ✅ Даст вам уникальный URL (например: `iqtest-123.netlify.app`)

---

## 🔄 Как обновлять сайт

### Вариант 1: Автоматически (после настройки)

Просто делайте `git push` - Netlify автоматически обновит сайт!

```bash
cd C:\Users\User\Desktop\iq
git add .
git commit -m "Описание изменений"
git push
```

**Netlify автоматически:**
- Обнаружит изменения
- Запустит новый деплой
- Обновит сайт за 1-2 минуты

### Вариант 2: Вручную через Netlify Dashboard

1. Откройте ваш сайт в [Netlify Dashboard](https://app.netlify.com/)
2. Перейдите в **"Deploys"**
3. Нажмите **"Trigger deploy"** → **"Deploy site"**

---

## 📝 Настройка кастомного домена (опционально)

1. В Netlify Dashboard → **"Domain settings"**
2. Нажмите **"Add custom domain"**
3. Введите ваш домен (например: `iqtest.ru`)
4. Следуйте инструкциям по настройке DNS

---

## ⚙️ Дополнительные настройки

### Переменные окружения (если нужны)

1. **Site settings** → **Environment variables**
2. Добавьте переменные (например: `API_KEY=value`)

### Настройка формы (если будет форма)

1. **Site settings** → **Forms**
2. Netlify автоматически обрабатывает формы

### Настройка редиректов

Создайте файл `_redirects` в корне проекта:

```
# Редиректы
/old-page /new-page 301
```

---

## 🔍 Проверка деплоя

После каждого `git push`:

1. Откройте Netlify Dashboard
2. Перейдите в **"Deploys"**
3. Увидите статус деплоя:
   - 🟢 **Published** - сайт обновлен
   - 🟡 **Building** - идет сборка
   - 🔴 **Failed** - ошибка (проверьте логи)

---

## 🐛 Решение проблем

### Сайт не обновляется автоматически

1. Проверьте, что репозиторий подключен:
   - **Site settings** → **Build & deploy** → **Continuous Deployment**
   - Должен быть указан ваш GitHub репозиторий

2. Проверьте настройки GitHub:
   - Убедитесь, что вы делаете `git push` в ветку `main`
   - Проверьте, что репозиторий публичный (или у Netlify есть доступ)

### Ошибки при деплое

1. Откройте **"Deploys"** → выберите неудачный деплой
2. Нажмите **"Deploy log"** - там будет информация об ошибке
3. Частые проблемы:
   - Неправильный путь к файлам
   - Ошибки в коде
   - Проблемы с зависимостями

---

## 📱 Быстрая команда для обновления

Создайте файл `update.bat` в папке проекта:

```batch
@echo off
cd C:\Users\User\Desktop\iq
git add .
git commit -m "Update site"
git push
echo.
echo Сайт обновляется в Netlify...
echo Проверьте статус: https://app.netlify.com/
pause
```

Теперь просто запускайте `update.bat` для обновления сайта!

---

## 🎯 Рекомендации

1. **Всегда делайте `git push`** после изменений
2. **Проверяйте статус деплоя** в Netlify Dashboard
3. **Используйте осмысленные сообщения коммитов** - они видны в истории деплоев
4. **Настройте уведомления** в Netlify для получения email о статусе деплоев

---

## 🔗 Полезные ссылки

- [Netlify Dashboard](https://app.netlify.com/)
- [Netlify Docs](https://docs.netlify.com/)
- [GitHub Repository](https://github.com/shzcrxbc9y-ops/iqtest)

---

**Важно:** После первого подключения GitHub к Netlify, все последующие обновления происходят автоматически при каждом `git push`!
